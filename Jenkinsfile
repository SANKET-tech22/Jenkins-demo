pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building from GitHub Jenkinsfile...'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing from GitHub Jenkinsfile...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying from GitHub Jenkinsfile...'
            }
        }
    }
}
